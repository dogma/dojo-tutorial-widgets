<h1>Listing pages</h1>

<table>
  <tr>
    <th colspan='2'>Title</th>
    <th>Tutorial</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @pages.each do |page| %>
  <tr>
    <td>Step <%= page.step %>:</td>
    <td><%= page.title %></td>
    <td><%= page.tutorial %></td>
    <td><%= link_to 'Show', page %></td>
    <td><%= link_to 'Edit', edit_page_path(page) %></td>
    <td><%= link_to 'Destroy', page, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Page', new_page_path %>
